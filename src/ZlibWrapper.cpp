#include "zlib.h"
#include "string.h"
#include <string>

using namespace std;

#ifndef zlib_wrapper_h
#define zlib_wrapper_h

/**
 * @brief 
 * 
 * Compress data using the zlib library.
 * 
 */
class ZlibWrapper
{
public:
    void write(string str)
    {
    }
    string read()
    {
    }
};

#endif